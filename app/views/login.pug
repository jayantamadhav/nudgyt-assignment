div.d-flex.justify-content-center.align-items-center(style="height: 100vh")
    div.login-container.p-3
        div.text-primary(style="font-size: 2rem") Login
        form(role="form", method="POST", action="/login")
            div.mt-4
                input.form-control(type="text", placeholder="Username", name="username", required)
            div.mt-3
                input.form-control(type="password", placeholder="Password", name="password", required)
            div.my-2.d-flex.justify-content-end.align-items-center
                button.my-3.btn.btn-primary(type="submit") Login
                    i.feather-16(data-feather="chevron-right")
        div(style="width: 100%; border-top: 2px solid #ccc")
            span Dont have an account ? 
                a(href="/register") Register now!